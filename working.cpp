#include "working.h"
#include "global.h"

working::working() {}

//void working::muon_sach(string id_book_, string id_user_, string booking_date, string pay_date) {
    //string id_ = borrow_data.find_new_id_borrow();
    //borrow b(id_, id_book_, acc_sign_in.getAccout_id(), "NULL", booking_date, "NULL", pay_date, "Đã đặt", "NULL", "0");
    //borrow_data.insert_Borrow(b);
    //ghi_borrow(borrow_data);
//}